import { DemoSessionControls } from './DemoSessionControls';

/** Compact demo session switcher for the widget header. */
export function DemoResetButton() {
  return <DemoSessionControls compact />;
}
